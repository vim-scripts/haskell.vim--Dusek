This is a mirror of http://www.vim.org/scripts/script.php?script_id=2641

Variation of the original `haskell.vim` that recognizes `for` as a keyword (due to its use in standalone deriving declarations).
